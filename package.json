{
  "name": "ai-plant-disease-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir backend",
    "frontend": "cd frontend && npm run dev",
    "dev:backend": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir backend",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
